'use client';
import {useEffect,useState} from 'react';
export default function HandpickConsole(){
 const [form,setForm]=useState<any>({quantity:1,decision:'pending'});const [message,setMessage]=useState('');const [error,setError]=useState('');const [items,setItems]=useState<any[]>([]);
 const load=async()=>{const r=await fetch('/api/operations?type=handpick',{cache:'no-store'});const d=await r.json();if(r.ok)setItems(d.data||[])};useEffect(()=>{load()},[]);
 const save=async(e:any)=>{e.preventDefault();setMessage('');setError('');const r=await fetch('/api/operations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'handpick',...form})});const d=await r.json();if(!r.ok){setError(d.error||'Could not save handpick item');return}setMessage(`Handpick captured: ${d.data.product_name}. It is not yet inventory — purchase approval remains an admin decision.`);setForm({quantity:1,decision:'pending'});load()};
 return <section className="panel" style={{marginBottom:18}}><div className="row"><div><h2 style={{margin:0}}>Admin Handpick Console</h2><p style={{margin:'4px 0',color:'#64748b'}}>Capture a product once at the supplier. No office-style re-entry later.</p></div><span className="badge">Admin only</span></div>{(message||error)&&<div className={error?'panel danger':'panel'} style={{marginTop:12}}>{error||message}</div>}<form onSubmit={save} className="formgrid" style={{marginTop:12}}><input className="input" placeholder="Barcode / QR" value={form.barcode||''} onChange={e=>setForm({...form,barcode:e.target.value})}/><input className="input" placeholder="Product name" required value={form.product_name||''} onChange={e=>setForm({...form,product_name:e.target.value})}/><input className="input" placeholder="Brand" value={form.brand||''} onChange={e=>setForm({...form,brand:e.target.value})}/><input className="input" placeholder="Model" value={form.model||''} onChange={e=>setForm({...form,model:e.target.value})}/><input className="input" placeholder="Serial number" value={form.serial_no||''} onChange={e=>setForm({...form,serial_no:e.target.value})}/><input className="input" type="number" min="1" placeholder="Quantity" value={form.quantity} onChange={e=>setForm({...form,quantity:e.target.value})}/><input className="input" type="number" min="0" placeholder="Observed market price" value={form.market_price||''} onChange={e=>setForm({...form,market_price:e.target.value})}/><input className="input" type="number" min="0" placeholder="Target maximum buy price" value={form.target_buy_price||''} onChange={e=>setForm({...form,target_buy_price:e.target.value})}/><select className="input" value={form.decision} onChange={e=>setForm({...form,decision:e.target.value})}><option value="pending">Pending decision</option><option value="buy">Buy</option><option value="buy_below">Buy below target</option><option value="skip">Skip</option></select><input className="input" placeholder="Condition / defects / quick notes" value={form.observed_condition||''} onChange={e=>setForm({...form,observed_condition:e.target.value})}/><button className="btn">Capture handpick item</button></form>{items.length>0&&<div style={{overflowX:'auto',marginTop:18}}><table className="table"><thead><tr><th>Product</th><th>Barcode</th><th>Qty</th><th>Market</th><th>Max buy</th><th>Decision</th><th>Condition</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.product_name}<br/><small>{x.brand||''} {x.model||''}</small></td><td>{x.barcode||'—'}</td><td>{x.quantity}</td><td>₹{Number(x.market_price||0).toLocaleString('en-IN')}</td><td>₹{Number(x.target_buy_price||0).toLocaleString('en-IN')}</td><td>{x.decision}</td><td>{x.observed_condition||'—'}</td></tr>)}</tbody></table></div>}</section>
}
