import {NextResponse} from 'next/server';import PDFDocument from 'pdfkit';import {supabaseAdmin} from '@/lib/serverSupabase';import {currentUser,can} from '@/lib/auth';
export const runtime='nodejs';const money=(n:any)=>`₹${Number(n||0).toLocaleString('en-IN',{minimumFractionDigits:2,maximumFractionDigits:2})`;
async function settings(sb:any){const {data}=await sb.from('business_settings').select('key,value_json').in('key',['business_name','legal_name','gstin','address','state','pincode','phone','email','terms']);const out:any={};for(const x of data||[])out[x.key]=x.value_json;return out;}
export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){try{const u=await currentUser();const internal=req.headers.get('x-internal-invoice')===process.env.APP_SESSION_SECRET;if((!u||!can(u.role,'sales'))&&!internal)return NextResponse.json({error:'Forbidden'},{status:403});const {id}=await params;const sb=supabaseAdmin();const [{data:sale,error:se},{data:items,error:ie},cfg]=await Promise.all([sb.from('sales').select('*,customers(name,mobile,town)').eq('id',id).single(),sb.from('sale_items').select('*,products(product_name,brand,model,serial_no)').eq('sale_id',id).order('id'),settings(sb)]);if(se)throw se;if(ie)throw ie;if(!cfg.gstin)return NextResponse.json({error:'GSTIN is not configured in Settings. Add the business GSTIN before issuing GST invoices.'},{status:400});const doc=new PDFDocument({size:'A4',margin:42});const chunks:Buffer[]=[];doc.on('data',(c)=>chunks.push(c));const done=new Promise<Buffer>(resolve=>doc.on('end',()=>resolve(Buffer.concat(chunks))));doc.fontSize(18).text(cfg.business_name||cfg.legal_name||'Latur Liquidation OS');doc.fontSize(9).text(cfg.address||'');doc.text(`GSTIN: ${cfg.gstin} | State: ${cfg.state||'Maharashtra'} | PIN: ${cfg.pincode||''}`);if(cfg.phone)doc.text(`Phone: ${cfg.phone}`);if(cfg.email)doc.text(`Email: ${cfg.email}`);doc.moveDown();doc.fontSize(16).text('TAX INVOICE',{align:'center'});doc.moveDown();doc.fontSize(10).text(`Invoice No: ${sale.invoice_no}`).text(`Invoice Date: ${new Date(sale.sale_date).toLocaleDateString('en-IN')}`);doc.moveDown();const c=sale.customers||{};doc.fontSize(10).text(`Bill To: ${sale.billing_name||c.name||'Customer'}`).text(`Mobile: ${c.mobile||''}`).text(`Address: ${sale.billing_address||c.town||''}`).text(`GSTIN/UIN: ${sale.billing_gstin||'Unregistered'}`).text(`Place of Supply: ${sale.place_of_supply||cfg.state||'Maharashtra'}`);doc.moveDown();let y=doc.y;doc.fontSize(7.5).text('Description',42,y,{width:155}).text('HSN',197,y,{width:42}).text('Qty',239,y,{width:30}).text('Rate',269,y,{width:60}).text('Taxable',329,y,{width:65}).text('CGST',394,y,{width:48}).text('SGST',442,y,{width:48}).text('IGST',490,y,{width:48}).text('Total',538,y,{width:55});y+=18;doc.moveTo(42,y).lineTo(593,y).stroke();let taxableTotal=0,cgstTotal=0,sgstTotal=0,igstTotal=0,total=0;for(const it of items||[]){const p=it.products||{};const taxable=Number(it.taxable_value||it.line_total||0),cgst=Number(it.cgst||0),sgst=Number(it.sgst||0),igst=Number(it.igst||0),line=taxable+cgst+sgst+igst;taxableTotal+=taxable;cgstTotal+=cgst;sgstTotal+=sgst;igstTotal+=igst;doc.fontSize(7.5).text(`${p.product_name||'Product'}${p.brand?' - '+p.brand:''}`,42,y,{width:155}).text(it.hsn_code||'',197,y,{width:42}).text(String(it.qty||1),239,y,{width:30}).text(money(it.unit_price),269,y,{width:60}).text(money(taxable),329,y,{width:65}).text(money(cgst),394,y,{width:48}).text(money(sgst),442,y,{width:48}).text(money(igst),490,y,{width:48}).text(money(line),538,y,{width:55});y+=18;if(y>700){doc.addPage();y=60}}total=taxableTotal+cgstTotal+sgstTotal+igstTotal;doc.moveDown(2);doc.fontSize(9).text(`Taxable Value: ${money(sale.subtotal||taxableTotal)}`,{align:'right'});doc.text(`CGST: ${money(cgstTotal)}`,{align:'right'});doc.text(`SGST: ${money(sgstTotal)}`,{align:'right'});doc.text(`IGST: ${money(igstTotal)}`,{align:'right'});doc.fontSize(13).text(`Grand Total: ${money(sale.total||total)}`,{align:'right'});doc.moveDown();doc.fontSize(9).text(`Payment: ${sale.payment_mode||'—'} | Status: ${sale.payment_status||'—'}`);doc.text('Reverse Charge: No');doc.moveDown(2);doc.text(cfg.terms||'Goods sold subject to stated warranty and return terms.');doc.moveDown(2);doc.text('For the supplier');doc.text('Authorised Signatory');doc.end();const pdf=await done;return new NextResponse(new Uint8Array(pdf),{headers:{'Content-Type':'application/pdf','Content-Disposition':`inline; filename="${sale.invoice_no||'invoice'}.pdf"`}});}catch(e:any){return NextResponse.json({error:e.message||'Invoice generation failed'},{status:500})}}
