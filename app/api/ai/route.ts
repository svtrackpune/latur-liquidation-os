import { NextResponse } from 'next/server';
import OpenAI from 'openai';
export async function POST(req:Request){try{const {message}=await req.json();if(!process.env.OPENAI_API_KEY)return NextResponse.json({reply:'AI is not connected yet. Add OPENAI_API_KEY in the deployment environment.'});const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const r=await client.responses.create({model:'gpt-5-mini',input:[{role:'system',content:'You are the operating AI for a Latur open-box liquidation business. Pilot capital ₹600000; initial inventory ceiling ₹450000; target A/B 70%, max C 20%, experimental 10%. Core categories: Kitchen Appliances 35%, Home/Personal Appliances 25%, Tools/Electrical 20%, Home Utility/Lifestyle 20%. Evaluate supplier quotes using landed cost, expected recovery, quality, defect risk, warranty, MOQ and cash-flow risk. Be concise and decision-oriented.'},{role:'user',content:message}]});return NextResponse.json({reply:r.output_text});}catch(e){return NextResponse.json({error:'AI request failed'},{status:500})}}
